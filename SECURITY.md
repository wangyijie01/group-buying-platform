# Security Policy

## 支持范围

本仓库是学习与求职展示项目，不承诺生产级安全支持。维护者会优先处理当前 `main` 分支中可复现的凭据泄露、身份校验绕过、支付回调伪造、SQL 注入和消息重放等问题。

## 报告漏洞

请不要在公开 Issue 中粘贴真实凭据、个人数据或可直接利用的攻击载荷。优先通过 GitHub 仓库的 **Security → Report a vulnerability** 私密报告；若该入口不可用，请只创建不含敏感细节的 Issue，请求维护者建立私密沟通渠道。

报告中建议包含受影响版本、复现条件、影响范围和最小化验证步骤。请勿攻击不属于报告者的系统或数据。

## 凭据与演示数据

- `.env.example` 只包含示例值，真实 `.env` 已被 Git 忽略。
- 支付能力默认关闭；本地启用时只使用沙箱账号。
- 发现疑似泄露凭据时，应先吊销并轮换，再清理 Git 历史和相关部署环境。
