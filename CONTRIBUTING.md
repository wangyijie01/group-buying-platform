# 贡献指南

感谢改进本项目。提交前请先阅读 [NOTICE.md](NOTICE.md)，保留原始实现者与当前维护范围的说明。

## 本地流程

1. 从 `main` 创建短生命周期分支。
2. 复制 `.env.example` 为 `.env`，不要提交真实凭据。
3. 完成修改并补充与风险匹配的测试。
4. 在仓库根目录运行 `mvn -B -ntp verify`。
5. 若修改 `docs/`，同时检查浅色页面在桌面与移动宽度下的可读性。

## 提交要求

- 一个提交聚焦一个可解释的变化。
- 业务注释说明约束、决策或失败处理，不重复代码字面含义。
- 新配置必须支持环境变量覆盖，并在 `.env.example` 中给出无敏感信息的示例。
- 涉及消息消费时说明幂等键、重试上限和失败出口。
- 未提供可复现实验前，不在 README 或简历 bullet 中加入性能数字。

## Pull Request 清单

- [ ] 根聚合构建通过
- [ ] 新增或更新自动化测试
- [ ] 配置与文档同步
- [ ] 未包含密钥、令牌、私钥或个人数据
- [ ] 来源与贡献边界表达准确
