spring:
  config:
    name: group-buying-market-app
  profiles:
    active: dev

# 补偿任务允许在不同环境按流量与下游承载能力调整扫描频率。
group-buying:
  notify:
    cron: '${GROUP_BUYING_NOTIFY_CRON:0 */1 * * * ?}'
  timeout-refund:
    cron: '${GROUP_BUYING_TIMEOUT_REFUND_CRON:0 */5 * * * ?}'
